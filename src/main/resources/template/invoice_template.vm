<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Invoice</title>
    <style>
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            font-size: 15px;
            color: #222;
            background: #fff;
            margin: 0;
        }
        .container {
            width: 800px;
            margin: 40px auto;
            padding: 0;
            border: 1px solid #ddd;
            box-shadow: 0 2px 12px #f2f2f2;
            background: #fff;
        }
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 30px 40px 20px 40px;
            border-bottom: 1px solid #eee;
            position: relative;
        }
        .header-left {
            flex: 1;
            min-width: 0;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        .header-logo {
            display: flex;
            align-items: center;
            margin-left: 24px;
        }
        .company-logo {
            width: 128px;
            height: 128px;
            object-fit: cover;
            border-radius: 50%;
            border: 2px solid #eee;
            box-shadow: 0 2px 8px #f4f4f4;
            margin-right: 50px;
            margin-left: -10px;
        }
        .client {
            font-size: 28px;
            font-weight: 600;
        }
        .client-details {
            font-size: 16px;
            margin: 8px 0 0 0;
        }
        .invoice-title-row {
            margin-top: 16px;
            display: flex;
            flex-direction: column;
            gap: 6px;
        }
        .invoice-title {
            font-size: 22px;
            font-weight: 700;
            letter-spacing: 2px;
        }
        .invoice-phone {
            font-size: 18px;
            margin-top: 10px;
            color: #222;
        }
        .bill-location-section {
            display: flex;
            justify-content: space-between;
            padding: 28px 40px 0 40px;
            margin-bottom: 8px;
        }
        .bill-to, .location {
            width: 48%;
            font-size: 15px;
        }
        .bill-label, .location-label, .date-label {
            padding: 0 0 5px 0;
            font-size: 16px;
            font-weight: 600;
            letter-spacing: 1px;
            margin-bottom: 8px;
        }
        .bill-label, .location-label {
            border-bottom: 1px solid #eee;
            width: fit-content;
            padding-right: 12ch;
        }
        .bill-to, .date {
            width: 48%;
            font-size: 15px;
        }
        .date {
            min-width: 150px;
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
            margin-left: 24px;
        }
        .date-row {
            font-size: 14px;
            margin-bottom: 3px;
        }
        .items-table {
            width: 90%;
            margin: 30px auto 0 auto;
            border-collapse: collapse;
            font-size: 15px;
            background: #fff;
            border-left: 1.5px solid #ddd;
            border-right: 1.5px solid #ddd;
        }
        .items-table th {
            font-weight: 700;
            background: #b6daff;
            text-align: center;
            padding: 12px 8px;
            border-bottom: 1px solid #eee;
            border-left: 1.5px solid #eee;
            border-right: 1.5px solid #eee;
        }
        .items-table td {
            padding: 10px 4px;
            border-bottom: 1px solid #eee;
            text-align: left;
            border-left: 1.5px solid #eee;
            border-right: 1.5px solid #eee;
        }
        .items-table td:last-child,
        .items-table th:last-child {
            text-align: right;
        }
        .items-table tfoot tr td {
            font-weight: 600;
            background: #fff;
            text-align: right;
            border-top: 1px solid #eee;
            border-bottom: none;
            font-size: 16px;
            border-left: 1.5px solid #eee;
            border-right: 1.5px solid #eee;
        }
        .items-table .label {
            text-align: right;
            font-size: 0.75rem;
        }
        .items-table .value {
            text-align: right;
            width: 130px;
        }
        .items-table .bold {
            font-weight: 700;
            font-size: 1.15rem;
        }
        .empty-cell {
            border: none !important;
            background: none !important;
        }
        .footer {
            text-align: left;
            padding: 25px 40px 10px 40px;
            border-top: 1px solid #eee;
            font-size: 15px;
            color: #444;
        }
        .thanks {
            font-size: 17px;
            font-weight: 600;
            margin-top: 15px;
        }
        .terms {
            font-size: 15px;
            color: #333;
            margin-top: 10px;
        }
        .color-blue {
            color: #5b8cff;
        }
        .color-white {
            color: #ffffff;
        }
    </style>
</head>
<body>
<div class="container">
    <!-- Header -->
    <div class="header">
        <div class="header-left">
            <div class="client color-blue">$!company.name</div>
            <div class="client-details">$!company.address</div>
            <div class="client-details">$!company.website, $!company.email</div>
            <div class="invoice-title-row">
                <br><br>
                <div class="invoice-title color-blue">INVOICE #$!invoice.invoiceNumber</div>
                <div class="invoice-phone">$!company.phone</div>
            </div>
        </div>
        <img src="$!company.logoUrl" alt="Company Logo" class="company-logo"/>
    </div>

    <!-- Bill To and Location -->
    <div class="bill-location-section">
        <div class="bill-to">
            <div class="bill-label color-blue">BILL TO</div>
            $!invoice.customerName <br>
            $!client.name <br>
            $!client.address <br>
            $!client.phone, $!invoice.customerEmail
        </div>
        <div class="location">
            <div class="location-label color-blue">LOCATION</div>
            $!address.street <br>
            $!address.city, $!address.state $!address.zip <br>
            $!client.phone
        </div>
        <div class="date">
            <div class="date-label color-white">Date</div>
            <div class="date-row">Invoice Date: $!invoice.invoiceDate</div>
            <div class="date-row">Due Date: $!invoice.dueDate</div>
        </div>
    </div>
    <!-- Items Table -->
    <table class="items-table">
        <thead>
        <tr>
            <th>DESCRIPTION</th>
            <th>QTY</th>
            <th>UNIT PRICE</th>
            <th>TOTAL</th>
        </tr>
        </thead>
        <tbody>
            #foreach($item in $items)
            <tr>
                <td>$!item.name</td>
                <td style="text-align: center;">$!item.quantity</td>
                <td style="text-align: center;">$!item.price</td>
                <td>#set($lineTotal = $item.quantity * $item.price)$lineTotal</td>
            </tr>
            #end
        </tbody>
        <tfoot>
        <tr>
            <td class="empty-cell" colspan="2"></td>
            <td class="label">SUBTOTAL</td>
            <td class="value">$!invoice.subtotal</td>
        </tr>
        <tr>
            <td class="empty-cell" colspan="2"></td>
            <td class="label">DISCOUNT</td>
            <td class="value">0.00</td>
        </tr>
        <tr>
            <td class="empty-cell" colspan="2"></td>
            <td class="label">SUBTOTAL LESS DISCOUNT</td>
            <td class="value">$!invoice.subtotal</td>
        </tr>
        <tr>
            <td class="empty-cell" colspan="2"></td>
            <td class="label">TAX RATE</td>
            <td class="value">$!invoice.tax%</td>
        </tr>
        <tr>
            <td class="empty-cell" colspan="2"></td>
            <td class="label">TOTAL TAX</td>
            <td class="value">$!invoice.tax</td>
        </tr>
        <tr>
            <td class="empty-cell" colspan="2"></td>
            <td class="label bold color-blue">Balance Due</td>
            <td class="value bold">$!invoice.total</td>
        </tr>
        </tfoot>
    </table>

    <!-- Footer / Thank You and Terms -->
    <div class="footer">
        <div class="thanks">Thank you for your business!</div>
        <div class="terms">
            <strong>Terms & Instructions:</strong><br>
            #if($invoice.paymentInstructions && $invoice.paymentInstructions.trim() != "")
                $invoice.paymentInstructions
            #else
                Please make the payment via bank transfer within 30 days.
            #end
            <br>
            #if($invoice.termsAndConditions && $invoice.termsAndConditions.trim() != "")
                $invoice.termsAndConditions
            #else
                All sales are final. Goods sold are not returnable or exchangeable.
            #end
        </div>
    </div>
</div>
</body>
</html>
